name: Build

# Auto-trigger disabled — Claude-driven CI. Run manually via
# `gh workflow run build.yml -R romaine-life/fzt` when ready to cut a new
# release. Downstream consumers (fzt-frontend, fzt-terminal) are also
# Claude-driven — bump their own go.mod via each repo's dispatch.yml.
on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: windows
            goarch: amd64
            ext: .exe
          - goos: linux
            goarch: amd64
            ext: ""
          - goos: darwin
            goarch: arm64
            ext: ""
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Compute next version
        id: version
        run: |
          latest=$(gh release list -R ${{ github.repository }} --limit 1 --json tagName -q '.[0].tagName // empty' 2>/dev/null || echo "")
          if [ -z "$latest" ]; then
            echo "version=v0.2.0" >> $GITHUB_OUTPUT
          else
            patch=$(echo "$latest" | sed 's/v[0-9]*\.[0-9]*\.//')
            echo "version=v0.2.$((patch + 1))" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: go build -ldflags="-X github.com/romaine-life/fzt/render.Version=${{ steps.version.outputs.version }}" -o fzt${{ matrix.ext }} ./cmd/fzt
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
      - uses: actions/upload-artifact@v7
        with:
          name: fzt-${{ matrix.goos }}-${{ matrix.goarch }}
          path: fzt${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
      - name: Prepare release assets
        run: |
          mkdir release
          cp artifacts/fzt-windows-amd64/fzt.exe release/fzt-windows-amd64.exe
          cp artifacts/fzt-linux-amd64/fzt release/fzt-linux-amd64
          cp artifacts/fzt-darwin-arm64/fzt release/fzt-darwin-arm64
      - name: Create versioned release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          latest=$(gh release list -R ${{ github.repository }} --limit 1 --json tagName -q '.[0].tagName // empty')
          if [ -z "$latest" ]; then
            next="v0.2.0"
          else
            patch=$(echo "$latest" | sed 's/v[0-9]*\.[0-9]*\.//')
            next="v0.2.$((patch + 1))"
          fi
          gh release create "$next" release/* \
            -R ${{ github.repository }} \
            --title "$next" \
            --notes "Engine-only scoring CLI. $(git log --oneline -1)"
